/**
 * @enum
 */

export enum ButtonType {
    PRIMARY = "btn-primary",
    SECONDARY = "btn-secondary",
    DISABLED = "btn-disabled"
}

export enum TextboxType {
    NUMBER = "number",
    TEXT = "text",
    PASSWORD = "password",
    EMAIL = "email"
    DATE = "date"
}

export enum ModalHeaderType {
    START = "dialog-header-start",
    END = "dialog-header-end",
    CENTER = "dialog-header-center"
}

export enum ModalFooterType {
    START = "dialog-footer-start",
    CENTER = "dialog-footer-center",
    END = "dialog-footer-end",
}

export enum CardType {
    BORDERED = "card-bordered",
    SHADOWED = "card-shadowed",
    NOSHADOW = "card-noshadow"
}

export enum SpinnerType {
    PRIMARY = "#4B0082",
    SECONDARY = "#EDE6F3"
}

export enum ModalType {
    DEFAULT = "dialog-wrapper",
    NOTIFICATION = "dialog-wrapper-notification"
}